<%= image_tag @pin.image.url(:medum) %>

<p>
  <strong>Description:</strong>
  <%= @pin.description %>
</p>

<%= link_to 'Edit', edit_pin_path(@pin) %> |
<%= link_to 'Delete', @pin, method: :delete, data: { confirm: "Are you sure?" } %> |
<%= link_to 'Back', pins_path %>

